architectural entities & roles
    entities
        client
            web browser
                roles
                    request resources
                    handle responses
                        render received contents inside viewport
        [[API]]
            roles
                listen for requests from clients
                have endpoints fulfill requests
        database
            roles
                permanently store and retrieve app data

